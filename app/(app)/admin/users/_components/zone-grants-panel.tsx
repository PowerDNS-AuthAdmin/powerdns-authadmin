/**
 * app/(app)/admin/users/_components/zone-grants-panel.tsx
 *
 * Re-export shim — the implementation lives at
 * `app/(app)/admin/_components/zone-grants-panel.tsx` because the same
 * panel renders for both user and team principals.
 */

export { ZoneGrantsPanel } from "../../_components/zone-grants-panel";
